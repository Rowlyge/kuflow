services:
  postgres:
    image: postgres:16
    container_name: proxy-postgres

    restart: unless-stopped

    env_file:
      - .env.production

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  kuflow:
    build: .
    container_name: kuflow

    restart: unless-stopped

    env_file:
      - .env

    depends_on:
      postgres:
        condition: service_healthy

    ports:
      - "18080:8080"

volumes:
  postgres_data: